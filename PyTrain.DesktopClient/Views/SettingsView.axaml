<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PyTrain.DesktopClient.ViewModels"
             xmlns:common="using:PyTrain.DesktopClient.Common"
             xmlns:theme="using:PyTrain.Libraries.Avalonia.Theming"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
             x:Class="PyTrain.DesktopClient.Views.SettingsView"
             x:DataType="vm:ISettingsViewModel">

  <StackPanel Spacing="16">
    
    <TextBlock Text="{x:Static common:Localization.Settings}" Classes="h1 Primary" />

    <StackPanel Spacing="8">
      <TextBlock Text="{x:Static common:Localization.Theme}" FontWeight="SemiBold" />
      
      <RadioButton GroupName="Theme"
                   Content="{x:Static common:Localization.Dark}"
                   IsChecked="{Binding IsDarkModeChecked}" />

      <RadioButton GroupName="Theme"
                   Content="{x:Static common:Localization.Light}"
                   IsChecked="{Binding IsLightModeChecked}" />

    </StackPanel>

  </StackPanel>

</UserControl>
